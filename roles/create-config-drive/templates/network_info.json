{
    "public_keys": {{ ssh_keys|to_json }}{% if hostname|default %},
    "name": {{ hostname|to_json }},
    "hostname": {{ hostname|to_json }}
    {% endif %},
    "links": {{ devnodes.links | to_json }},
    "networks": {{ devnodes.networks | to_json }},
    "services": [
        {
            "type": "dns",
            "address": "8.8.8.8"
        }
    ]
}
